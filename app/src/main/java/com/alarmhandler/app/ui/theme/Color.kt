package com.alarmhandler.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * The palette is sampled straight from the two provided images so the app and
 * the artwork are literally the same colours.
 */
object PixelPalette {
    /** Mascot body in the square artwork. */
    val Coral = Color(0xFFF25C45)

    /** Mascot body in the wide city artwork -- used for secondary accents. */
    val CoralMuted = Color(0xFFDA7757)
    val CoralDeep = Color(0xFFBE684D)
    val CoralPale = Color(0xFFF8D2C7)

    /** Sky of the wide artwork. */
    val OffWhite = Color(0xFFF0EFEB)

    /** Ground band of the wide artwork. */
    val Stone = Color(0xFFE7E6E1)
    val Paper = Color(0xFFFAF9F6)

    val Ink = Color(0xFF111111)
    val InkSoft = Color(0xFF4A4A48)

    val Night = Color(0xFF14120F)
    val NightSurface = Color(0xFF201D19)
    val NightRaised = Color(0xFF2C2823)
    val NightLine = Color(0xFF4A443C)

    val Danger = Color(0xFFB3261E)
    val Success = Color(0xFF3F7D4E)
}
