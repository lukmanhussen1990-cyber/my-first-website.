# my-first-website

Claude Opus 5 — concept advertisement.

Open `index.html` in a browser and click **Play with sound**. Use **Record video**
to export the 28-second ad as a video file (MP4 where supported, otherwise WebM).
