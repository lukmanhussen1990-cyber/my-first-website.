// STUB — Audio agent replaces the internals (keep the API from ARCHITECTURE.md).
export function createAudio() {
  return {
    unlock() {}, play() {}, setMusic() {}, setIntensity() {}, setListener() {},
    setVolume() {}, suspend() {}, resume() {},
  };
}
