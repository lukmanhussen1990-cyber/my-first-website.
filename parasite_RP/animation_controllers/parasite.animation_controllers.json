{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.pm_parasite.move": {
      "initial_state": "idle",
      "states": {
        "idle": {
          "animations": ["idle"],
          "blend_transition": 0.2,
          "transitions": [
            { "walk": "query.modified_move_speed > 0.05" }
          ]
        },
        "walk": {
          "animations": ["walk"],
          "blend_transition": 0.2,
          "particle_effects": [
            { "effect": "spore" }
          ],
          "transitions": [
            { "idle": "query.modified_move_speed <= 0.05" }
          ]
        }
      }
    }
  }
}
